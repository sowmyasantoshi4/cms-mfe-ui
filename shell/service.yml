apiVersion: v1
kind: Service
metadata:
  name: shell-service
spec:
  selector:
    app: shell  # Match the app label from the deployment
  ports:
    - protocol: TCP
      port: 80  # Exposed port
      targetPort: 80  # Port inside the pod
  type: LoadBalancer  # Expose to external traffic (use NodePort if internal)
