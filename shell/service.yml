apiVersion: v1
kind: Service
metadata:
  name: shell-service
spec:
  selector:
    app: shell-deployment  # Match the app label from the deployment
  ports:
    - protocol: TCP
      port: 9000  # Exposed port
      targetPort: 9000  # Port inside the pod
  type: LoadBalancer  # Expose to external traffic (use NodePort if internal)
