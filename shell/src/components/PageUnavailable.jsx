import React, { Suspense, lazy } from 'react';

export const PageUnavailable = () => (
  <div className="tracking-unavailable">
    <h2>Page Unavailable</h2>
    <p>We are currently working on this. Please check back later after sometime !</p>
  </div>
);
