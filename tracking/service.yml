apiVersion: v1
kind: Service
metadata:
  name: login-service
spec:
  selector:
    app: login  # Match the app label from the deployment
  ports:
    - protocol: TCP
      port: 9003  # Exposed port
      targetPort: 9003  # Port inside the pod
  type: LoadBalancer  # Expose to external traffic (use NodePort if internal)
